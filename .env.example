# Google Calendar (optional). Restart the app after changing.
#
# Setup in Google Cloud Console (console.cloud.google.com):
# 1. Create/select a project and enable the "Google Calendar API".
# 2. Configure the OAuth consent screen (add test users while in testing mode).
# 3. Create three OAuth 2.0 client IDs (APIs & Services → Credentials):
#    a. Web application  → paste the client ID below as WEB_CLIENT_ID
#    b. Android           → package: com.gapwalk.app + your signing SHA-1
#    c. iOS               → bundle ID: com.gapwalk.app
# 4. Do NOT reuse the web client ID for iOS or Android.
#
# The app uses @react-native-google-signin/google-signin (native SDK).
# This does NOT work in Expo Go — use a development build (npx expo run:android).
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your_web_client_id.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=your_android_client_id.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=your_ios_client_id.apps.googleusercontent.com

# Google Maps API key for Android map rendering (required for Walking map screen).
# Use the same key value for both variables.
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key
GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Auth0 (required for app login gate). Restart the app after changing.
EXPO_PUBLIC_AUTH0_DOMAIN=your-tenant.auth0.com
EXPO_PUBLIC_AUTH0_CLIENT_ID=your_mobile_client_id
EXPO_PUBLIC_AUTH0_AUDIENCE=https://api.gapwalk.com

# Backend API URL (required for research data sync).
# Development: http://localhost:3000
# Production: https://api.yourdomain.com
EXPO_PUBLIC_API_URL=http://localhost:3000
